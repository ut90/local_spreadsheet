{
  "name": "local-spreadsheet",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "concurrently -k -n VITE,MAIN,PRE,ELC -c blue,magenta,cyan,green \"vite\" \"tsc -w -p tsconfig.main.json\" \"tsc -w -p tsconfig.preload.json\" \"wait-on tcp:5173 dist/main/main.js dist/preload/index.js && electron .\"",
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && vite build",
    "start": "electron .",
    "lint": "echo 'add ESLint later'",
    "typecheck": "tsc -p tsconfig.renderer.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.7.4",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^2.1.1",
    "concurrently": "^9.0.1",
    "electron": "^31.3.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.6",
    "vitest": "^2.1.1",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "diff": "^5.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "yaml": "^2.5.1"
  }
}
